# SoulEcho - AI 灵魂伴侣

<div align="center">

![SoulEcho](https://img.shields.io/badge/SoulEcho-AI%E7%81%B5%E9%AD%82%E4%BC%98%E5%86%A5-blue)
![React](https://img.shields.io/badge/React-18.3-61DAFB?style=flat-square&logo=react)
![TypeScript](https://img.shields.io/badge/TypeScript-5.0-3178C6?style=flat-square&logo=typescript)
![TailwindCSS](https://img.shields.io/badge/TailwindCSS-3.4-38B2AC?style=flat-square&logo=tailwind-css)
![FastAPI](https://img.shields.io/badge/FastAPI-0.109-009688?style=flat-square&logo=fastapi)
![Python](https://img.shields.io/badge/Python-3.10+-3776AB?style=flat-square&logo=python)

*一个基于AI的虚拟伴侣应用，让心灵共鸣成为可能*

</div>

---

## ✨ 功能特性

- **用户认证** - JWT登录/注册系统，登录状态持久化
- **个性化创建** - AI辅助生成名字和外貌特征，自定义性格、爱好
- **智能对话** - SSE流式输出，实时响应，沉浸式聊天体验
- **聊天历史** - 分页加载、关键词搜索、导出功能
- **记忆系统** - AI自动提取和总结记忆，长期存储加深羁绊
- **语音对话** - Edge TTS语音合成、语音输入、多种语音选择
- **多角色支持** - 创建多个不同类型的AI伴侣
- **用户资料** - 个人资料管理、账号设置、查看统计数据

---

## 🏗️ 技术架构

```
┌─────────────────────────────────────────────────────────┐
│                      Frontend (React)                   │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌────────┐  │
│  │  Vite    │  │ Tailwind │  │  Framer  │  │ Axios  │  │
│  │ TypeScript│   Motion   │             │        │  │
│  └──────────┘  └──────────┘  └──────────┘  └────────┘  │
│                      ┌──────────────────┐              │
│                      │ React Router     │              │
│                      └──────────────────┘              │
└────────────────────────┬────────────────────────────────┘
                         │ REST API / SSE
┌────────────────────────┴────────────────────────────────┐
│                      Backend (FastAPI)                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌────────┐  │
│  │ OpenAI   │  │ ChromaDB │  │ SQLite   │  │ Edge  │  │
│  │   API    │  │(可选)    │  │          │  │  TTS  │  │
│  └──────────┘  └──────────┘  └──────────┘  └────────┘  │
└─────────────────────────────────────────────────────────┘
```

---

## 📁 项目结构

```
soul-echo/
├── backend/                 # FastAPI 后端服务
│   ├── app/
│   │   ├── main.py         # 应用入口
│   │   ├── api/            # API 路由
│   │   │   ├── auth.py         # 认证接口
│   │   │   ├── characters.py   # 角色管理 API
│   │   │   ├── chat.py         # 对话 API
│   │   │   ├── memory.py       # 记忆 API
│   │   │   └── tts.py          # TTS 语音 API
│   │   ├── models/         # 数据模型
│   │   ├── services/       # 业务逻辑
│   │   │   ├── memory.py      # AI记忆分析服务
│   │   │   └── tts.py         # 语音合成服务
│   │   └── db/             # 数据库
│   ├── requirements.txt
│   └── .env.example
│
├── frontend/               # React 前端应用
│   ├── src/
│   │   ├── components/     # 公共组件
│   │   │   ├── ui/         # 基础 UI 组件
│   │   │   ├── chat/       # 聊天相关
│   │   │   │   ├── ChatInput.tsx      # 输入框
│   │   │   │   ├── VoiceInput.tsx     # 语音输入
│   │   │   │   └── VoiceSettings.tsx  # 语音设置
│   │   │   └── character/  # 角色相关
│   │   ├── pages/          # 页面
│   │   │   ├── Login/      # 登录注册
│   │   │   ├── Home/       # 首页
│   │   │   ├── Create/     # 创建角色
│   │   │   ├── Chat/       # 聊天页面
│   │   │   ├── Memories/   # 记忆管理
│   │   │   └── Profile/    # 个人中心
│   │   ├── hooks/          # 自定义 Hooks
│   │   │   └── useChat.ts  # 对话逻辑
│   │   ├── services/       # API 服务
│   │   ├── stores/         # 状态管理
│   │   ├── types/          # TypeScript 类型
│   │   └── utils/          # 工具函数
│   ├── package.json
│   ├── tailwind.config.js
│   ├── vite.config.ts
│   └── tsconfig.json
│
├── docker-compose.yml      # Docker 部署配置
├── README.md               # 项目说明
└── LICENSE                 # 开源协议
```

---

## 🚀 快速开始

### 前置要求

- Node.js 18+
- Python 3.10+
- OpenAI API Key

### 1. 克隆项目

```bash
git clone https://github.com/skydzx/soul-echo.git
cd soul-echo
```

### 2. 后端设置

```bash
cd backend
cp .env.example .env
# 编辑 .env 添加 API Key

# 创建虚拟环境
python -m venv venv
# Windows: venv\Scripts\activate

# 安装依赖
pip install -r requirements.txt

# 启动服务
uvicorn app.main:app --reload
```

后端服务运行在: `http://localhost:8000`

### 3. 前端设置

```bash
cd frontend

# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

前端服务运行在: `http://localhost:5173`

### 4. Docker 部署（可选）

```bash
docker-compose up -d
```

---

## 📚 API 文档

启动后端服务后，访问 `http://localhost:8000/docs` 查看 Swagger 文档。

### 主要接口

| 方法 | 路径 | 描述 |
|------|------|------|
| POST | `/api/auth/register` | 用户注册 |
| POST | `/api/auth/login` | 用户登录 |
| GET | `/api/characters` | 获取所有角色 |
| POST | `/api/characters` | 创建新角色 |
| GET | `/api/characters/{id}` | 获取角色详情 |
| DELETE | `/api/characters/{id}` | 删除角色 |
| POST | `/api/chat` | 发送消息获取回复 (SSE) |
| GET | `/api/chat/history/{character_id}` | 获取聊天历史 |
| GET | `/api/memories/{character_id}` | 获取角色记忆 |
| POST | `/api/tts` | 语音合成 |

---

## 📅 更新日志

### v1.0 (2025-02) - 已完成

- [x] 用户认证系统 (JWT登录/注册)
- [x] 角色创建 (AI生成名字/外貌, 头像上传)
- [x] 智能对话 (SSE流式, 快捷回复, 表情包)
- [x] 聊天历史 (分页, 搜索, 导出)
- [x] 记忆系统 (AI提取, 长期记忆)
- [x] 语音对话 (TTS, 语音输入, 语音设置)
- [x] 用户资料 (个人中心, 账号设置)

### 规划中

- [ ] 角色亲密度系统
- [ ] 多人对话/群聊
- [ ] 实时语音通话
- [ ] 角色分享社区
- [ ] 移动端适配

---

## 🤝 贡献

欢迎贡献代码！请提交 Pull Request。

---

## 📄 许可证

本项目采用 MIT 许可证。

---

<div align="center">

*SoulEcho - 让每一次对话都有意义*

</div>
